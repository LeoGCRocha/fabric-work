fabric:
  cas:
  - "ca1.orgD.com"
  peers: 
  - "peer1.orgD.com"
  - "peer2.orgD.com"
  settings:
    ca:
      FABRIC_LOGGING_SPEC: INFO
      adminPassword: "teste"
    peer:
      FABRIC_LOGGING_SPEC: INFO
    orderer:
      FABRIC_LOGGING_SPEC: INFO
  netname: "site_D"
  container_options: "--restart=always --log-opt max-size=10m --log-opt max-file=3"