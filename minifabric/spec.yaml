fabric:
  cas:
  - "ca1.orgname.acadblock.br"
  peers:
  - "peer1.orgname.acadblock.br"
  - "peer2.orgname.acadblock.br"
  settings:
    ca:
      FABRIC_LOGGING_SPEC: ERROR
    peer:
      FABRIC_LOGGING_SPEC: ERROR
    orderer:
      FABRIC_LOGGING_SPEC: ERROR
  netname: "mysite"
  # endpoint_address: xxx.xxx.xx.xxx
  container_options: "--restart=always --log-opt max-size=10m --log-opt max-file=3"