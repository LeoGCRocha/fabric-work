
fabric:
  cas:
  - "ca1.orgC.com"
  peers: 
  - "peer1.orgC.com"
  - "peer2.orgC.com"
  settings:
    ca:
      FABRIC_LOGGING_SPEC: INFO
    peer:
      FABRIC_LOGGING_SPEC: INFO
    orderer:
      FABRIC_LOGGING_SPEC: INFO
  netname: "site_C"
  container_options: "--restart=always --log-opt max-size=10m --log-opt max-file=3"