fabric:
  cas:
  - "ca1.orgE.com"
  peers: 
  - "peer1.orgE.com"
  settings:
    ca:
      FABRIC_LOGGING_SPEC: ERROR
      adminPassword: "teste"
    peer:
      FABRIC_LOGGING_SPEC: ERROR
    orderer:
      FABRIC_LOGGING_SPEC: ERROR
  netname: "site_E"
  container_options: "--restart=always --log-opt max-size=10m --log-opt max-file=3"